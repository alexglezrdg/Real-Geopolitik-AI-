# QUICK REFERENCE - DEPLOY NOW

**Status:** ✅ All tests passed  
**Ready:** YES  
**Time to deploy:** < 5 minutes  

---

## 🚀 STEP-BY-STEP DEPLOYMENT

### 1. Commit changes
```bash
cd "/Users/alexgonzalez/Youtube WorkSpace/geopolitik-x-autopost"
git add -A
git commit -m "Production: hardening + event fingerprinting + topic tier"
git log --oneline -1
```

**Expected:** Shows commit hash (e.g., `a3f2c1e Production: hardening...`)

---

### 2. Install cron entry
```bash
crontab -e
```

**Paste this line (at end of file):**
```cron
0 * * * * cd "/Users/alexgonzalez/Youtube WorkSpace/geopolitik-x-autopost" && ./scripts/autopost-hourly.sh >> logs/cron.log 2>&1
```

**Save:** `Ctrl+O` → `Enter` → `Ctrl+X`

---

### 3. Verify cron installed
```bash
crontab -l | grep autopost
```

**Expected output:**
```
0 * * * * cd "/Users/alexgonzalez/Youtube WorkSpace/geopolitik-x-autopost" && ./scripts/autopost-hourly.sh >> logs/cron.log 2>&1
```

---

## 📊 MONITORING

### Monitor live logs (real-time)
```bash
cd "/Users/alexgonzalez/Youtube WorkSpace/geopolitik-x-autopost"
tail -f logs/autopost-hourly.log
```

### Check if cron ran (after first hour)
```bash
tail -20 logs/cron.log
```

### Check for hard geopolitics (TIER 1 boost)
```bash
grep "TIER 1" logs/autopost-hourly.log
```

### Check for duplicates blocked (event fingerprinting)
```bash
grep "Duplicate event\|Event FP" logs/autopost-hourly.log
```

---

## 🔍 QUICK TESTS

### Test 1: Verify cron environment works
```bash
env -i HOME="$HOME" PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin" \
  bash scripts/autopost-hourly.sh 2>&1 | tail -5
```

**Expected:** npm run dev executes (no PATH errors)

### Test 2: Verify TypeScript compiles
```bash
npx tsc --noEmit && echo "✅ 0 errors"
```

**Expected:** `✅ 0 errors`

### Test 3: Verify state file has fingerprint field
```bash
jq '.recentEventFingerprints' out/curator_state.json
```

**Expected:** `[]` (empty array on first run)

---

## 📰 POST MANUALLY (to test immediately)

### Test with OTAN story (TIER 1)
```bash
npm run dev -- --url "https://www.nato.int/" --live
```

**Expected:** 
- Score should be ~101+ (TIER 1 NATO boost)
- Should post successfully

### Monitor to see if duplicates get blocked
```bash
# After posting same event with different headline
npm run dev -- --live 2>&1 | grep -i "duplicate\|event fp"

# Should show -45 penalty for same event
```

---

## 🎯 SUCCESS INDICATORS

✅ **After first cron run (1 hour from now):**
- [ ] logs/cron.log has timestamp entry
- [ ] logs/autopost-hourly.log shows [SUCCESS] or [SKIP]
- [ ] curator_state.json shows 1 event in recentEventFingerprints
- [ ] X post was made (check Twitter)

✅ **After second run (2 hours from now):**
- [ ] Different story posted (not same event)
- [ ] If same event was in RSS → should see -45 penalty
- [ ] curator_state.json now has 2 events

✅ **After TIER 1 story appears in RSS:**
- [ ] OTAN/sanciones/energía story gets +20 boost
- [ ] Shows up in logs as "TIER 1" 
- [ ] Gets posted before lower-tier stories

---

## ⚠️ IF SOMETHING BREAKS

### Check logs
```bash
tail -50 logs/autopost-hourly.log
tail -50 logs/cron.log
```

### Test manually
```bash
cd "/Users/alexgonzalez/Youtube WorkSpace/geopolitik-x-autopost"
./scripts/autopost-hourly.sh
```

### Verify environment
```bash
command -v node && node -v
command -v npm && npm -v
which node
which npm
```

### Rollback cron if needed
```bash
crontab -e
# Delete the line with autopost-hourly.sh
# Save and exit
```

---

## 📋 CHECKLIST

Before deploying:
- [x] All tests passed (TypeScript, bash, cron)
- [x] Event fingerprinting implemented
- [x] Topic tier gate working
- [x] curator_state.json migrated

Ready to deploy:
- [ ] git commit done
- [ ] crontab -e completed
- [ ] crontab -l verified
- [ ] First log entry appears

---

## 🎬 ACTION ITEMS

**NOW (< 5 min):**
1. `git add -A && git commit -m "Production deploy"`
2. `crontab -e` + paste cron line + save
3. `crontab -l | grep autopost` to verify

**IN 1 HOUR:**
1. `tail -f logs/cron.log` to check execution
2. `tail -f logs/autopost-hourly.log` for details
3. Check X for new post

**ONGOING:**
1. Monitor for TIER 1 stories in logs
2. Verify event fingerprints blocking duplicates
3. Track engagement on X posts

---

## 🔗 REFERENCE DOCS

- **HARDENING-LEVEL-2.md** - What changed in cron/timeout
- **TOPIC-GATE-IMPLEMENTATION.md** - How event FP + tier work
- **VALIDATION-COMPLETE.md** - Full test results
- **VERIFIED-NEWS-3.md** - 3 suggested stories (OTAN, Venezuela, Canada)

---

**Ready?** Yes → Execute steps 1-3 above  
**Questions?** Check reference docs or test manually  
**Status:** 🟢 PRODUCTION READY

